import { BentoGrid, BentoGridItem } from "/components/ui/bento-grid.tsx";
import { projectItems } from "/pages/project_items.js"
import Link from 'next/link'

# project & open-source contributions 💻

Explore a curated selection of my projects and open-source contributions, showcasing innovation and collaboration in action.

_(ps: detailed pages to-be-added)_

<br></br>

<BentoGrid className="max-w-4xl mx-auto md:auto-rows-[20rem]">
      {projectItems.map((item, i) => (
        <Link href={item.link}>
            <BentoGridItem
                key={i}
                title={item.title}
                description={item.description}
                header={item.header}
                className={item.className}
                icon={item.icon}
            />
        </Link>
      ))}
</BentoGrid>